name: Deploy MCP Server (Manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Note
        run: |
          echo "MCP server is deployed manually via Docker on the Hetzner server."
          echo "See docs/MCP_SERVER.md for deployment instructions."
          echo ""
          echo "To deploy:"
          echo "  ssh hetzner-ax41-1"
          echo "  cd ~/mcp-server-deploy/mcp-server"
          echo "  git pull"
          echo "  docker compose up -d --build"
