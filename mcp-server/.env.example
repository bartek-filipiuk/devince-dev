# MCP Server Authentication
# Token that Claude Code / MCP clients use to connect to this server
# Generate with: openssl rand -hex 32
MCP_AUTH_TOKEN=your-mcp-auth-token-here

# External API Authentication
# Must match EXTERNAL_API_TOKEN configured on devince.dev
EXTERNAL_API_TOKEN=your-external-api-token-here

# Base URL of the devince.dev instance (no trailing slash)
DEVINCE_BASE_URL=https://devince.dev
